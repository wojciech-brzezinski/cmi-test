{% extends "base.html.twig" %}

{% block javascripts %}
    {{ encore_entry_script_tags("article") }}
{% endblock %}

{% block body %}
    <main>
        <article>
            <h1>{{ article.title }}</h1>

            <section>{{ article.text }}</section>
        </article>

        <h3>Comments:</h3>
        <section data-section-comments="{{ article.id }}"></section>
    </main>
{% endblock %}
